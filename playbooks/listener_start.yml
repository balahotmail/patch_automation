---
# playbooks/listener_start.yml
# Usage:
#   ansible-playbook playbooks/listener_start.yml -l prod_19c_apr2026
- name: Start the TNS listener
  hosts: all
  gather_facts: true
  roles:
    - listener_start
